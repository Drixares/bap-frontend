const CategoryDescription = ({ children }: { children: React.ReactNode }) => {
    return <p className="text-slate-700 font-medium text-xl max-w-2xl">{children}</p>;
};

export default CategoryDescription;
